package day35.interfaces;

public abstract class Device implements Runnable {

    // When an abstract class implements an interface it compiles fine
    // because an abstract class inherits interface's abstract methods as its own

    // a class can only IMPLEMENT an interface
    // an interface can EXTEND another interface
}
